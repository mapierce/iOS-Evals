# Held-out prompt split — never committed. Publishing it rots the benchmark.
prompts.local/

# Machine-generated ground truth. Always re-derived from the SDK on disk.
build/

# SwiftPM build products in the scaffold.
.build/
*.pyc
__pycache__/
.venv/
.DS_Store

# API keys. Never committed — see .env.example for the required variables.
.env
.env.local
